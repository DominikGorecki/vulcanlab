# .Cursor

The cursor files that re useful for this repo. `.cursor` is in the gitignore so that it remains clean for everyone, but if you're using linux type system like Ubuntu, Mac OS, or even WSL, you can symbolically link the file from the relative `.cursor` folder.

