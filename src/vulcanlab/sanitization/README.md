# Sanitization
The sanitization step works with the `works` table in the DB so all CLI and modules are based on the Work ID -- a specific row in the `works` table




files:
{
    original_pdf:
    original_markdown:
}