Q1) What permissions/access control should collections have? Should all users see all collections, or should collections be user-specific or workspace-specific?

A. All users can see and edit all collections (shared collections)
B. Each collection is owned by a specific user (private collections)
C. Collections can be either shared or private, with explicit sharing controls
Freeform: Specify your preferred permissions model or if collections have no access control in this phase

---

Q2) When adding an item to a collection from the "Add to Collection" button, should the user be able to add a custom note immediately in the modal, or should the modal only allow selecting/creating a collection and notes are added later from the collection details page?

A. Add note immediately in the modal (requires textarea in modal)
B. Only select/create collection in modal, edit notes later on details page
C. Both options: optional note field in modal, editable later
Freeform: Specify your preference for the add-to-collection workflow

---

Q3) For the link column in collection_items, should we validate that the links match the expected URL patterns (e.g., must start with /search/result/ or /rag/), or allow any arbitrary string/URL?

A. Validate links to ensure they match expected patterns for the item types
B. Store links as free-form text, no validation
C. Store structured data (work_id, line numbers, etc.) separately and construct links dynamically
Freeform: Specify your approach for link storage and validation

---

Q4) Should there be a limit on how many items a collection can hold, or should collections scale to any size?

A. No limit, collections can hold unlimited items
B. Soft limit (e.g., warn at 100+ items but allow)
C. Hard limit (e.g., maximum 500 items per collection)
Freeform: Specify your preference for collection size constraints

---

Q5) For the item type field in collection_items, what should we call this column (item_type, content_type, type) and should we store it as a string (VARCHAR) to avoid enum issues mentioned, or use a different approach?

A. Use VARCHAR with name "item_type" (values: "excerpt", "research_result", "research_query")
B. Use VARCHAR with name "content_type" (values: "excerpt", "research_result", "research_query")
C. Use VARCHAR with name "type" (values: "excerpt", "research_result", "research_query")
Freeform: Specify preferred column name, type, and approach for storing item types

---

Q6) Should collections have metadata like tags, categories, or only name and description?

A. Only name and description (as specified)
B. Add tags (array/JSON field) for better organization
C. Add both tags and a category/folder system
Freeform: Specify additional metadata needed for collections

---

Q7) Should the collection details page allow bulk operations (e.g., delete multiple items, reorder multiple items at once), or only single-item operations?

A. Only single-item operations (edit note, edit order one at a time)
B. Add bulk delete functionality
C. Add bulk delete and bulk reorder functionality
Freeform: Specify what bulk operations are needed

---

Q8) When showing the "order" column, should the table allow sorting by the order field, or is the order field itself the primary sort that overrides table sorting?

A. Order field is used as default sort, but users can sort by other columns
B. Order field is the ONLY sort (table sorting disabled or only sorts within same order value)
C. Order field is just another data field, table sorting is independent
Freeform: Specify how order and table sorting should interact

---

Q9) For the "Add to Collection" modal, should it show all collections or have search/filter for large numbers of collections?

A. Show all collections in a simple list (works for small numbers)
B. Add search/filter for collection name
C. Add search/filter and pagination for very large numbers
Freeform: Specify how the modal should handle displaying collections

---

Q10) Should there be any automatic metadata captured when adding items to collections (e.g., date added, who added it), or just the user-provided note and order?

A. Only user-provided note and order (as specified)
B. Add date_added timestamp automatically
C. Add date_added and last_modified timestamps
Freeform: Specify what automatic metadata should be tracked