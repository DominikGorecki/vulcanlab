# VulcanLab Environment Configuration Template
# Copy this file to .env and fill in your actual values

# ============================================================
# Database Passwords (REQUIRED)
# ============================================================
# These passwords are required for database connections
# The application will fail to start if these are not set

# PostgreSQL admin user password (for database initialization)
POSTGRES_ADMIN_PASSWORD=your_admin_password_here

# PostgreSQL application user password (for normal operations)
POSTGRES_APP_PASSWORD=your_app_password_here

# ============================================================
# LLM API Keys
# ============================================================
# At least one LLM API key is required for text generation

# OpenAI API key (for GPT models)
LLM_OPENAI_API_KEY=your_openai_api_key_here

# Google API key (for Gemini models)
LLM_GOOGLE_API_KEY=your_google_api_key_here

# ============================================================
# Setup Instructions
# ============================================================
# 1. Copy this file: cp .env.example .env
# 2. Edit .env and replace placeholder values with your actual credentials
# 3. Never commit .env to version control (it's in .gitignore)
# 4. For validation, run: python -m vulcanLab.data.validate_config_cli
#
# For more information, see README.md section "Secrets Configuration (.env)"
